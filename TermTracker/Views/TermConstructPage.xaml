<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:TermTracker.CustomRender"
             x:Class="TermTracker.Views.TermConstructPage">
    <ContentPage.Content>
        <StackLayout>
            <StackLayout>
                <Label 
                    Text="Term" 
                    Style="{StaticResource modalTitle}"
                 />
            </StackLayout>
            <Entry Placeholder="Term Title" x:Name="termTitle"/>
            <!--<local:CustomStartDatePicker />
            <local:CustomEndDatePicker />-->
            <Label Text="Start Date"/>
            <DatePicker x:Name="startDateSelected"/>
            <Label Text="End Date"/>
            <DatePicker x:Name="endDateSelected"/>
            <StackLayout>
                <Button Text="Save" Clicked="SaveButton_Clicked"/>
                <Button Text="Cancel" Clicked="CancelButton_Clicked"/>
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>